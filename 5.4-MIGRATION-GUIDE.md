# Added Cobalt Script Command Registry
1. Regex replace
    ```CommandRegistrationCallback\.EVENT\.register\s*\{\s*[^>]+->\s*dispatcher\.register\(([\s\S]*?return@executes\s*-?\d+[\s\}\)]*?)\)\s*\}```
   with
    ```CobaltScriptCommandRegistry.register($1)```
2. Add the following import if any match was found
    ```import io.github.jeefdevelopment.cobalt.event.CobaltScriptCommandRegistry```